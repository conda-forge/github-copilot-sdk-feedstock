schema_version: 1

context:
  name: github-copilot-sdk
  version: "1.0.13"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://files.pythonhosted.org/packages/py3/${{ name[0] }}/${{ name }}/github_copilot_sdk-${{ version }}-py3-none-any.whl
  sha256: 941dd5b55cf32ba55c73c651052a4a52b259b470c68bf6a6ac3d240c235402c9

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install github*whl -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools >=45
    - wheel
    - setuptools-scm >=6.2
    - pip
  run:
    - python >=${{ python_min }}
    - httpx
    - python-dateutil >=2.9.0.post0
    - pydantic >=2.0
    - typing_extensions >=4.0.0

tests:
  - python:
      imports:
        - copilot
      pip_check: true
      python_version:
        - ${{ python_min }}
        - "*"

about:
  summary: Python SDK for GitHub Copilot CLI
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/github/copilot-sdk

extra:
  recipe-maintainers:
    - akrherz
